function createPoint(a, b){
    var point ={
        x: a, 
        y:b
    }
    return point;
}


function distance(point1, point2){ 
    var point1 = {x: a, y: b}
    var point2 = {x: c, y: d}
    return Math.abs(point1) + Math.abs(point2);
}